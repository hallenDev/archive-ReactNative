const LoginInformationChanged = ({ route }) => {
  // const code = route?.params?.code
  // const email = route?.params?.email
  // const type = route?.params?.type
  return null
}

export default LoginInformationChanged
